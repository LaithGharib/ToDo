// performing a task
function greet(person, lastName) {
  console.log("Hello " + person + " " + lastName);
}

//calaculating a value
function square(number) {
  return number * number;
}

let number = square(2);
console.log(number);

user = "Sam";

greet(user, "Wilson");

user = "John";

greet(user, "Boy");
